package org.example.SOLID.Open_Close_Principle;

public class Main {
    /*
    * Software entities (classes , modules, methods etc...)
    * should be open for extension, but closed for modification.
    * Open for Extension : it should Extend existing behavior
    * Closed for Modification:  Existing code remains unchanged.
    *
    *  In java, we have base class if we want to extend base then we should be able to do it,
    *  but we should not change anything in based class( or we should never have needed to
    *  change anything in base class, so base class should be ideal always).
    * */


}
